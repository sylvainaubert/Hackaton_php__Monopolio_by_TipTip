<form method="post" action="{{ path('refund_delete', {'id': refund.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ refund.id) }}">
    <div class="text-center">
    <a type="button" class="btn btn-danger">Supprimer</a>
    </div>
</form>
